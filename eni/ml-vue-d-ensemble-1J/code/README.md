Labs
===

Pour lancer ces notebooks jupyter :

- créer un [environnement virtuel](https://virtualenv.pypa.io/) et l'activer

- lancer la commande `pip install -r requirements.txt` dans ce répertoire

- lancer le serveur jupyter avec la commande `jupyter notebook`
