%% for k, v in config.items()
\newcommand{\\V{k | camelcase}}{\V{v}}
%% endfor

\PassOptionsToPackage{dvipsnames,hyperref,usenames,table}{xcolor}

\documentclass[
  hyperref={hidelinks},
  \PresentationSize\BLOCK{if handout},handout\BLOCK{endif}
  ]{beamer}

\usepackage{formation}

%% if print:
\renewcommand{\bluelink}[2]{%
  {%
    \color{blue}%
    \url{#1}%
  }%
}
\usepackage{pgfpages}
\pgfpagesuselayout{2 on 1}[a4paper]
%% endif

\setbeamertemplate{frame footer}{\CompanyName{} ⋅ \DeckTitle{} ⋅ CC BY-SA 4.0}

\author{\vfill\TrainerName}
\institute{%
  \vfill%
  \includegraphics[height=\CompanyLogoHeight]{\CompanyLogo}%
  \newline%
  \url{\CompanyWebsite}%
}
\date{}
\title{\DeckTitle}

\begin{document}

\NoAutoSpacing

\maketitle

%% if toc is defined and toc:
%% if "toc_en" in config:
\begin{frame}{Table of Contents}
  %% else:
\begin{frame}{Table des matières}
%%endif
%% if "toc_size" in config:
  \\V{config["toc_size"]}{\tableofcontents[subsectionstyle=hide]}
%% else:
  \tableofcontents[subsectionstyle=hide]
%% endif
\end{frame}
%% endif

%% for part in target.parts:
%% if part.title != None and parts | length > 1:
\begin{frame}[standout]
  \V{part.title}
\end{frame}
%% endif
%% for section in part.sections:
%% if section.title is string:
%% if section.level < 3:
\\V{"sub" * section.level}section{\V{section.title}}
%% endif
%% else:
\input{\V{section}}
%% endif
%% endfor
%% endfor

\end{document}
