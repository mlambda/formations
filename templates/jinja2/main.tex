%% for k, v in config.items()
\newcommand{\\VAR{k | camelcase}}{\VAR{v}}
%% endfor

\documentclass[
  xcolor={dvipsnames,hyperref,usenames,table},
  hyperref={hidelinks},
  \PresentationSize\BLOCK{if handout},handout\BLOCK{endif}
  ]{beamer}

\usepackage{formation}

%% if print:
\renewcommand{\bluelink}[2]{%
  {%
    \color{blue}%
    \url{#1}%
  }%
}
%% endif

\setbeamertemplate{frame footer}{\CompanyName{} --- \DeckTitle}

\author{\vfill\TrainerName}
\institute{%
  \vfill%
  \includegraphics[height=\CompanyLogoHeight]{\CompanyLogo}%
  \newline%
  \url{\CompanyWebsite}%
}
\date{}
\title{\DeckTitle}
\subtitle{\VAR{target.title}}

\begin{document}

\NoAutoSpacing

\maketitle

%% if "programme" not in config or config["programme"]:
\begin{frame}{Programme}
\tableofcontents[subsectionstyle=hide]
\end{frame}
%% endif

%% for section in target.sections:
%% if section.title != None:
\section{\VAR{section.title}}
%% endif
%% for section_input in section.inputs:
%% if section_input.title != None:
\subsection{\VAR{section_input.title}}
%% endif
\input{\VAR{section_input.path}}
%% endfor
%% endfor

\end{document}
