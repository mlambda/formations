\documentclass[beamer,crop,tikz]{standalone}

\usepackage{formation}

\begin{document}
  \begin{tikzpicture}
    \datavisualization [scientific axes=clean,
                        all axes=grid,
                        x axis={unit length=1cm, label=$x$, ticks and grid={step=1}},
                        y axis={unit length=1cm, label=$\ReLU(x)$, ticks and grid={step=1}},
                        visualize as line/.list={relu},
                        relu={style={red, line width=0.5mm}}]
    data [set=relu] {
      x, y
      -4, 0
      0, 0
      4, 4
    };
  \end{tikzpicture}
\end{document}
