\documentclass[beamer,crop,tikz]{standalone}

\usepackage{formation}
\usepackage{tikz}

\usetikzlibrary{positioning}

\begin{document}
\begin{tikzpicture}
	\node[rectangle] (Y0) at (0, 0) {$\dots$};
	\node[hencoder,rectangle, draw, right=2em of Y0, minimum height=1cm, minimum width=1cm] (RNN) {RNN$_{GD}$};
	\node[hencoder,rectangle, right=4em of RNN, draw, minimum height=1cm, minimum width=1cm] (RNN2) {RNN$_{GD}$};
	\node[hencoder,rectangle, right=4em of RNN2, draw, minimum height=1cm, minimum width=1cm] (RNN3) {RNN$_{GD}$};
			
	\node[hencoder,rectangle, right=4em of RNN3, draw, minimum height=1cm, minimum width=1cm] (RNN4) {RNN$_{GD}$};
	\node[rectangle, right=2em of RNN4] (RNN5) {$\dots$};
			
			
	\node[hencoder,rectangle, above=of RNN4, draw, minimum height=1cm, minimum width=1cm] (R25) {RNN$_{DG}$};
	\node[hencoder,rectangle, left=4em of R25, minimum height=1cm, minimum width=1cm, draw] (R24) {RNN$_{DG}$};
	\node[hencoder,rectangle, left=4em of R24, draw, minimum height=1cm, minimum width=1cm] (R23) {RNN$_{DG}$};
	\node[hencoder,rectangle, left=4em of R23, draw, minimum height=1cm, minimum width=1cm] (R22) {RNN$_{DG}$};
	\node[rectangle, left=2em of R22] (R21) {$\dots$};
	\node[right=2em of R25] (Y20) {$\dots$};
			
	\node[input,below=of RNN] (X1) {$\vec{x}_2$};
	\node[input,below=of RNN2] (X2) {$\vec{x}_3$};
	\node[input,below=of RNN3] (X3) {$\vec{x}_4$};
	\node[input,below=of RNN4] (X4) {$\vec{x}_5$};
	\node[output,above=of R25] (Y5) {[$\vec{o}_5^{DG}$;$\vec{o}_5^{GD}$]};
	\node[output,above=of R24] (Y4) {[$\vec{o}_4^{DG}$;$\vec{o}_4^{GD}$]};
	\node[output,above=of R23] (Y3) {[$\vec{o}_3^{DG}$;$\vec{o}_3^{GD}$]};
	\node[output,above=of R22] (Y2) {[$\vec{o}_2^{DG}$;$\vec{o}_2^{GD}$]};
			
	\draw[-stealth, thick] (X1) -- (RNN);
	\draw[-stealth, thick] (X2) -- (RNN2);
	\draw[-stealth, thick] (X3) -- (RNN3);
	\draw[-stealth, thick] (X4) -- (RNN4);
	\draw[-stealth, thick, densely dotted] (Y0) -- (RNN);
	\draw[-stealth, thick] (RNN)  --  (RNN2);
	\draw[-stealth, thick] (RNN2) --  (RNN3);
	\draw[-stealth, thick] (RNN3) --  (RNN4);
	\draw[-stealth, densely dotted, thick] (RNN4) -- (RNN5);
	\node[below=4em of Y0] (d) {\dots};
	\node[below=4em of RNN5] (d) {\dots};
			
	\path[-stealth, ultra thick, white] (X1) edge[bend left=45] (R22);
	\path[-stealth, thick] (X1) edge[bend left=45] (R22);
	\path[-stealth, ultra thick, white] (X2) edge[bend left=45] (R23);
	\path[-stealth, thick] (X2) edge[bend left=45] (R23);
	\path[-stealth, ultra thick, white] (X3) edge[bend left=45] (R24);
	\path[-stealth, thick] (X3) edge[bend left=45] (R24);
	\path[-stealth, ultra thick, white] (X4) edge[bend left=45] (R25);
	\path[-stealth, thick] (X4) edge[bend left=45] (R25);
	\draw[-stealth, densely dotted, thick] (Y20) -- (R25);
	\draw[stealth-, densely dotted, thick] (R21) -- (R22);
    
	\draw[stealth-, thick] (R22) -- (R23);
	\draw[stealth-, thick] (R23) -- (R24);
	\draw[stealth-, thick] (R24) -- (R25);
	\draw[-stealth, densely dotted, thick] (Y20) -- (R25);	
        
    \draw[-stealth, thick] (R22) -- node[left, pos=0.5] {$\vec{o}_2^{DG}$} (Y2);
    \draw[-stealth, thick] (R23) -- node[left, pos=0.5] {$\vec{o}_3^{DG}$} (Y3);
    \draw[-stealth, thick] (R24) -- node[left, pos=0.5] {$\vec{o}_4^{DG}$} (Y4);
    \draw[-stealth, thick] (R25) -- node[left, pos=0.5] {$\vec{o}_5^{DG}$} (Y5);
			
	\path[-stealth, thick] (RNN)  edge[bend right=45] node[right, pos=0.85] {$\vec{o}_2^{GD}$} (Y2);
	\path[-stealth, thick] (RNN2) edge[bend right=45] node[right, pos=0.85] {$\vec{o}_3^{GD}$} (Y3);
	\path[-stealth, thick] (RNN3) edge[bend right=45] node[right, pos=0.85] {$\vec{o}_4^{GD}$} (Y4);
	\path[-stealth, thick] (RNN4) edge[bend right=45] node[right, pos=0.85] {$\vec{o}_5^{GD}$} (Y5);
			
\end{tikzpicture}
\end{document}